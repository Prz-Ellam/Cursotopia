
export const createCategory = function(event) {
    event.preventDefault();
}

export const updateCategory = function(event) {
    event.preventDefault();
}

export const deleteCategory = function(event) {
    event.preventDefault();
}